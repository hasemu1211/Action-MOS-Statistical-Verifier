*.unitypackage filter=lfs diff=lfs merge=lfs -text
*.fbx filter=lfs diff=lfs merge=lfs -text
*.onnx filter=lfs diff=lfs merge=lfs -text
# 1. 유니티 프로젝트 내의 모든 파일을 일단 통계에서 제외 (Vendored 처리)
UnityProject/** linguist-vendored

# 2. 하지만 그 안의 C# 코드(.cs)만은 명확하게 다시 살려내기
UnityProject/**/*.cs linguist-detectable linguist-vendored=false

# 3. 루트에 있는 파이썬 코드 등은 당연히 포함
*.py linguist-detectable